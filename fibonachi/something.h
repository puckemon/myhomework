#ifndef MY_PROJECT
#define MY_PROJECT

namespace mp {
	long long fibonachi(long long); //type a number - get a fibonacci number with that index
	void testpointers(int& , int); //just testing how pointers work with functions
}
#endif